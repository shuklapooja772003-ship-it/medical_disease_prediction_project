# Medical Disease Prediction System

Educational Python/ML project using a synthetic symptom dataset and Random Forest classification.

**Safety:** This is NOT a medical diagnostic device. Outputs are model predictions for academic demonstration only.

Run:
```bash
pip install -r requirements.txt
python src/create_dataset.py
python src/train_model.py
python src/predict.py
```
